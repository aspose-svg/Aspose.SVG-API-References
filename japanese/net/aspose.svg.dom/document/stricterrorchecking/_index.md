---
title: "Document.StrictErrorChecking"
second_title: ".NET 用 Aspose.SVG API リファレンス"
description: "Document StrictErrorChecking プロパティ。エラーチェックを強制するかどうかを指定する属性です。false に設定された場合、実装は通常 DOM 操作で定義されているすべてのエラーケースをテストせず、DOM 操作時に DOMException を発生させず、Document.normalizeDocument を使用する際にエラーを報告しない自由があります。エラーが発生した場合の動作は未定義です。この属性はデフォルトで true です。"
type: docs
weight: 240
url: /ja/net/aspose.svg.dom/document/stricterrorchecking/
---
## Document.StrictErrorChecking property

エラーチェックを強制するかどうかを指定する属性です。false に設定された場合、実装は通常 DOM 操作で定義されるすべてのエラーケースをテストせず、DOM 操作で DOMException を発生させず、Document.normalizeDocument() 使用時にエラーを報告しない自由があります。エラーが発生した場合の動作は未定義です。この属性はデフォルトで true です。

```csharp
public bool StrictErrorChecking { get; set; }
```

### Property Value

厳密なエラーチェックが有効な場合は `true`、それ以外は `false`。

### 参照

* class [Document](../)
* namespace [Aspose.Svg.Dom](../../../aspose.svg.dom/)
* assembly [Aspose.SVG](../../../)
