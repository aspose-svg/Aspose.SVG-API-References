---
title: CreateNSResolver
second_title: Référence de l'API Aspose.SVG pour .NET
description: Adapte nimporte quel nœud DOM pour résoudre les espaces de noms afin quune expression XPath puisse être facilement évaluée par rapport au contexte du nœud où elle est apparue dans le document. Cet adaptateur fonctionne comme la méthode DOM niveau 3lookupNamespaceURI sur les nœuds en résolvant le namespaceURI à partir dun préfixe donné en utilisant les informations actuelles disponibles dans la hiérarchie du nœud au moment où lookupNamespaceURI est appelé en résolvant également correctement le préfixe xml implicite.
type: docs
weight: 910
url: /fr/net/aspose.svg.dom/document/creatensresolver/
---
## Document.CreateNSResolver method

Adapte n'importe quel nœud DOM pour résoudre les espaces de noms afin qu'une expression XPath puisse être facilement évaluée par rapport au contexte du nœud où elle est apparue dans le document. Cet adaptateur fonctionne comme la méthode DOM niveau 3`lookupNamespaceURI` sur les nœuds en résolvant le namespaceURI à partir d'un préfixe donné en utilisant les informations actuelles disponibles dans la hiérarchie du nœud au moment où lookupNamespaceURI est appelé, en résolvant également correctement le préfixe xml implicite.

```csharp
public IXPathNSResolver CreateNSResolver(Node nodeResolver)
```

| Paramètre | Taper | La description |
| --- | --- | --- |
| nodeResolver | Node | Nœud à utiliser comme contexte pour la résolution d'espace de noms. |

### Return_Value

[`IXPathNSResolver`](../../../aspose.svg.dom.xpath/ixpathnsresolver) qui résout les espaces de noms en fonction des définitions dans la portée d'un nœud spécifié.

### Voir également

* interface [IXPathNSResolver](../../../aspose.svg.dom.xpath/ixpathnsresolver)
* class [Node](../../node)
* class [Document](../../document)
* espace de noms [Aspose.Svg.Dom](../../document)
* Assemblée [Aspose.SVG](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.SVG.dll -->
