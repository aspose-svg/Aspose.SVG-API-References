---
title: Key
second_title: Riferimento API Aspose.SVG per .NET
description: La chiave mantiene il valore chiave del tasto premuto. Se il valore ha una rappresentazione stampata DEVE essere una stringa di caratteri Unicode non vuota conforme allalgoritmo per determinare il valore chiave definito in questa specifica. Se il valore è una chiave di controllo che non ha una rappresentazione stampata DEVE essere uno dei valori chiave definiti nel set di valori chiave come determinato dallalgoritmo per determinare il valore della chiave. Le implementazioni che non sono in grado di identificare una chiave DEVONO utilizzare il valore della chiave Unidentified.
type: docs
weight: 60
url: /it/net/aspose.svg.dom.events/keyboardevent/key/
---
## KeyboardEvent.Key property

La chiave mantiene il valore chiave del tasto premuto. Se il valore ha una rappresentazione stampata, DEVE essere una stringa di caratteri Unicode non vuota, conforme all'algoritmo per determinare il valore chiave definito in questa specifica. Se il valore è una chiave di controllo che non ha una rappresentazione stampata, DEVE essere uno dei valori chiave definiti nel set di valori chiave, come determinato dall'algoritmo per determinare il valore della chiave. Le implementazioni che non sono in grado di identificare una chiave DEVONO utilizzare il valore della chiave Unidentified.

```csharp
public string Key { get; }
```

### Valore della proprietà

Il valore della chiave.

### Guarda anche

* class [KeyboardEvent](../../keyboardevent)
* spazio dei nomi [Aspose.Svg.Dom.Events](../../keyboardevent)
* assemblea [Aspose.SVG](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.SVG.dll -->
