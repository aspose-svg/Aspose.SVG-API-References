---
title: IWindow.Parent
second_title: Riferimento API Aspose.SVG per .NET
description: IWindow proprietà. Lattributo parent IDL sulloggetto Window di un Document in un contesto di navigazione b deve restituire loggetto WindowProxy del contesto di navigazione genitore se presente cioè se b è un contesto di navigazione figlio oppure loggetto WindowProxy del contesto di navigazione contesto b stesso altrimenti ad esempio se si tratta di un contesto di navigazione di primo livello o di un contesto di navigazione annidato distaccato.
type: docs
weight: 60
url: /it/net/aspose.svg.window/iwindow/parent/
---
## IWindow.Parent property

L'attributo parent IDL sull'oggetto Window di un Document in un contesto di navigazione b deve restituire l'oggetto WindowProxy del contesto di navigazione genitore, se presente (cioè se b è un contesto di navigazione figlio), oppure l'oggetto WindowProxy del contesto di navigazione contesto b stesso, altrimenti (ad esempio se si tratta di un contesto di navigazione di primo livello o di un contesto di navigazione annidato distaccato).

```csharp
public IWindow Parent { get; }
```

### Valore della proprietà

Il genitore.

### Guarda anche

* interface [IWindow](../)
* spazio dei nomi [Aspose.Svg.Window](../../iwindow/)
* assemblea [Aspose.SVG](../../../)


