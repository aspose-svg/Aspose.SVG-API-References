---
title: "Node.InsertBefore"
second_title: "Aspose.SVG for .NET API 参考"
description: "Node InsertBefore 方法。将在现有子节点 child 之前插入节点。如果 child 为 null，则将节点插入到子节点列表的末尾。如果 child 是 DocumentFragment 对象，则其所有子节点按相同顺序在 child 之前插入。如果该子节点已经在树中，则首先将其移除。"
type: docs
weight: 200
url: /zh/net/aspose.svg.dom/node/insertbefore/
---
## Node.InsertBefore method

在现有子节点 child 之前插入该节点。如果 child 为 null，则将节点插入到子节点列表的末尾。如果 child 是 DocumentFragment 对象，则其所有子节点按相同顺序在 child 之前插入。如果该子节点已经在树中，则会先将其移除。

```csharp
public Node InsertBefore(Node node, Node child)
```

| 参数 | 类型 | 描述 |
| --- | --- | --- |
| node | Node | 新的子节点。 |
| child | Node | 引用子节点。 |

### 返回值

返回已插入的节点

### 另请参阅

* class [Node](../)
* namespace [Aspose.Svg.Dom](../../../aspose.svg.dom/)
* assembly [Aspose.SVG](../../../)
