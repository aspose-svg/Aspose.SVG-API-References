---
title: "Document.StrictErrorChecking"
second_title: "Aspose.SVG for .NET API 参考"
description: "Document StrictErrorChecking 属性。一个指定是否强制进行错误检查的属性。当设置为 false 时，实现可以不检测 DOM 操作中通常定义的所有可能错误情况，也不会在 DOM 操作中抛出任何 DOMException，或在使用 Document.normalizeDocument 时报告错误。出现错误时行为未定义。此属性默认值为 true。"
type: docs
weight: 240
url: /zh/net/aspose.svg.dom/document/stricterrorchecking/
---
## Document.StrictErrorChecking property

一个属性，用于指定是否强制进行错误检查。当设置为 false 时，实现可以不检查 DOM 操作中通常定义的每一种可能错误情况，也不在 DOM 操作或使用 Document.normalizeDocument() 时抛出任何 DOMException 或报告错误。出现错误时，行为未定义。此属性默认值为 true。

```csharp
public bool StrictErrorChecking { get; set; }
```

### Property Value

`true` 表示启用严格错误检查；否则为 `false`。

### 另请参阅

* class [Document](../)
* namespace [Aspose.Svg.Dom](../../../aspose.svg.dom/)
* assembly [Aspose.SVG](../../../)
