name: Trigger Svg Japanese Production Build

on:
  push:
    branches: [ production ]
    paths: [ 'japanese/**' ]
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_TOKEN }}
          repository: Aspose/apireference.aspose.com
          event-type: build-svg-japanese-production
