name: Trigger Svg Italian Production Build

on:
  push:
    branches: [ production ]
    paths: [ 'italian/**' ]
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_TOKEN }}
          repository: Aspose/apireference.aspose.com
          event-type: build-svg-italian-production
