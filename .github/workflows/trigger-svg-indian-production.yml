name: Trigger Svg Indian Production Build

on:
  push:
    branches: [ production ]
    paths: [ 'indian/**' ]
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_TOKEN }}
          repository: Aspose/apireference.aspose.com
          event-type: build-svg-indian-production
