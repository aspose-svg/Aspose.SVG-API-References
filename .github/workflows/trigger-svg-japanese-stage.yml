name: Trigger SVG Japanese Stage Build

on:
  push:
    branches: [ main ]
    paths: [ 'japanese/**' ]
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.REPO_TOKEN }}
          repository: Aspose/apireference.aspose.com
          event-type: build-svg-japanese-stage
