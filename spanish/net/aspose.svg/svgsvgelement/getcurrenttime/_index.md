---
title: SVGSVGElement.GetCurrentTime
second_title: Referencia de API de Aspose.SVG para .NET
description: SVGSVGElement método. Devuelve la hora actual en segundos relativa a la hora de inicio del fragmento de documento SVG actual. Si se llama a getCurrentTime antes de que comience la línea de tiempo del documento por ejemplo mediante un script que se ejecuta en un elemento script antes de que se envíe el evento SVGLoad del documento se devuelve 0.
type: docs
weight: 200
url: /es/net/aspose.svg/svgsvgelement/getcurrenttime/
---
## SVGSVGElement.GetCurrentTime method

Devuelve la hora actual en segundos relativa a la hora de inicio del fragmento de documento SVG actual. Si se llama a getCurrentTime antes de que comience la línea de tiempo del documento (por ejemplo, mediante un script que se ejecuta en un elemento 'script' antes de que se envíe el evento SVGLoad del documento), se devuelve 0.

```csharp
public float GetCurrentTime()
```

### Valor_devuelto

El tiempo actual en segundos, o 0 si la línea de tiempo del documento aún no ha comenzado.

### Ver también

* class [SVGSVGElement](../)
* espacio de nombres [Aspose.Svg](../../svgsvgelement/)
* asamblea [Aspose.SVG](../../../)


