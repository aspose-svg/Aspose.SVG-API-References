---
title: "SVGSVGElement.GetCurrentTime"
second_title: "Aspose.SVG für .NET API-Referenz"
description: "SVGSVGElement GetCurrentTime‑Methode. Gibt die aktuelle Zeit in Sekunden zurück, relativ zur Startzeit des aktuellen SVG‑Dokumentfragments. Wenn getCurrentTime aufgerufen wird, bevor die Dokumentzeitlinie begonnen hat, zum Beispiel durch ein Skript, das in einem script‑Element ausgeführt wird, bevor das SVGLoad‑Ereignis des Dokuments gesendet wird, wird 0 zurückgegeben."
type: docs
weight: 200
url: /de/net/aspose.svg/svgsvgelement/getcurrenttime/
---
## SVGSVGElement.GetCurrentTime method

Gibt die aktuelle Zeit in Sekunden relativ zur Startzeit des aktuellen SVG‑Dokumentfragments zurück. Wenn getCurrentTime aufgerufen wird, bevor die Dokument‑Zeitlinie begonnen hat (z. B. durch ein Skript, das in einem ‘script’-Element läuft, bevor das SVGLoad‑Ereignis des Dokuments gesendet wird), wird 0 zurückgegeben.

```csharp
public float GetCurrentTime()
```

### Rückgabewert

Die aktuelle Zeit in Sekunden, oder 0, wenn die Dokumentzeitlinie noch nicht begonnen hat.

### Siehe auch

* class [SVGSVGElement](../)
* namespace [Aspose.Svg](../../../aspose.svg/)
* assembly [Aspose.SVG](../../../)
