---
title: "ErrorEvent.Error"
second_title: "Aspose.SVG für .NET API-Referenz"
description: "ErrorEvent Error property. Das error-Attribut muss den Wert zurückgeben, mit dem es initialisiert wurde. Wenn das Objekt erstellt wird, muss dieses Attribut auf null initialisiert werden. Gegebenenfalls wird es auf das Objekt gesetzt, das den Fehler repräsentiert, z. B. das Ausnahmeobjekt im Fall einer nicht abgefangenen DOM-Ausnahme."
type: docs
weight: 30
url: /de/net/aspose.svg.dom.events/errorevent/error/
---
## ErrorEvent.Error property

Das error-Attribut muss den Wert zurückgeben, mit dem es initialisiert wurde. Beim Erstellen des Objekts muss dieses Attribut auf null initialisiert werden. Bei Bedarf wird es auf das Objekt gesetzt, das den Fehler darstellt (z. B. das Ausnahmeobjekt im Fall einer nicht abgefangenen DOM-Ausnahme).

```csharp
public object Error { get; }
```

### Property Value

Der Fehler.

### Siehe auch

* class [ErrorEvent](../)
* namespace [Aspose.Svg.Dom.Events](../../../aspose.svg.dom.events/)
* assembly [Aspose.SVG](../../../)
